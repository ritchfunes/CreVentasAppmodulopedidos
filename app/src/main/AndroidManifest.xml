<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ritchiecristopher.myfirstapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/apptitle"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        android:debuggable="true"
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MostrarCliente"
            android:label="Categoria" />
        <activity
            android:name=".MostrarCategoria"
            android:label="Categoria" />
        <activity
            android:name=".UpdateCategoria"
            android:label="Actualizar Categoria" />
        <activity
            android:name=".MenuprincipalOriginal"
            android:label="@string/title_activity_menuprincipal_original"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Unidadedit"
            android:label="Unidad" />
        <activity
            android:name=".Showitem"
            android:label="Articulos" />
        <activity
            android:name=".Additem"
            android:label="Articulos" />
        <activity android:name=".Addsalesline"  android:label="Agregar pedido ventas" />
        <activity android:name=".Salesshow" />
        <activity
            android:name=".tabshowsalesorder"
            android:label="@string/title_activity_tabshowsalesorder"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PedidosFacturados"
            android:label="Detalle pedido facturado"></activity>
    </application>

</manifest>